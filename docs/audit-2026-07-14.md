# Full review & audit — pi-zai (2026-07-14)

**Repo:** `OnlineChefGroep/pi-zai` @ `main`  
**Verdict:** **WARN** — mechanical gates green; critical/high correctness and privacy-defense gaps found and fixed in this PR. Medium/low items deferred.

## Method

1. Mechanical gates: `npm ci`, `lint`, `build`, `test` (119), worker `check`
2. Parallel readonly lanes: security, correctness/bugs, docs/API drift
3. Fix-now for CRITICAL/HIGH; document deferred MEDIUM/LOW

## Mechanical gates

| Gate | Result |
|------|--------|
| `npm run lint` | Pass (warnings only — pre-existing `any` / style) |
| `npm run build` | Pass |
| `npm test` | Pass (119 tests) |
| worker `npm run check` | Pass |

## Findings fixed in this PR

| Sev | Area | Fix |
|-----|------|-----|
| CRITICAL | Tool-loop timing | Reset `headersReceivedAt` on each new provider request; clamp latencies ≥ 0 |
| HIGH | Session usage crash | Skip assistant messages without `usage` in `getSessionUsageTotals` |
| HIGH | Dropped attempts | Record attempts at `turn_end` without requiring `usage` |
| HIGH | Pending placeholders | Arm real request IDs when provider was never reached |
| HIGH | Orphan tool ends | Ignore `tool_execution_end` without matching `begin` |
| HIGH | Worker forbidden keys | Add `prompt` / `path` parity with client + test |
| HIGH | Local file modes | State dir `0o700`, SQLite/consent/secret `0o600` |
| HIGH | Docs drift | Standalone paths, thinking `xhigh`, affinity defaults, security fetch wording |
| HIGH | Stale plan | Archive `CURSOR_IMPLEMENTATION_PLAN.md` |
| MEDIUM | Error categories | HTTP status fallback in transport summary |
| MEDIUM | Shutdown leak | Reset correlation/attempt trackers on `session_shutdown` |

## Deferred (not blocking)

| Sev | Item | Notes |
|-----|------|-------|
| MEDIUM | Unauthenticated telemetry ingest | Rate-limited; needs ingest token / Access for production |
| MEDIUM | Custom `telemetry.ingestUrl` scheme allowlist | Require HTTPS |
| MEDIUM | Client validate weaker than worker | Share validation module |
| MEDIUM | Worker string blob max lengths | DoS/cost hardening |
| MEDIUM | Export path confinement | Reject absolute/`..` by default |
| MEDIUM | `attempt` conflates tool-loop rounds with retries | Separate counters |
| MEDIUM | Rollup loses tool / error-category detail | Schema extension |
| MEDIUM | `retryRateBucket` names errors | Rename or recompute |
| MEDIUM | Telemetry sync/upload integration tests | Expand coverage |
| MEDIUM | Publish workflow lighter than CI | Mirror lint + worker check |
| LOW | `sessionAffinity: observe` no distinct behavior | Implement or alias |
| LOW | `EXTENSION_VERSION` duplicated from package.json | Generate at build |
| LOW | Duplicate `isZaiProvider` / `bucketCount` | Deduplicate |

## What looks solid

- Dual telemetry gate (settings `aggregate` + consent file)
- No provider re-registration; Pi remains credential/model owner
- Boundary tests prove telemetry-off lifecycle stays local
- Parameterized SQLite; HMAC local project IDs; secrets never in aggregates
- Fail-open SQLite → memory storage

## Follow-ups

1. Optional ingest authentication for production worker
2. Separate `providerRound` vs transport-retry counters
3. Persist tool/error aggregates into daily rollups
4. Expand telemetry sync/upload tests
